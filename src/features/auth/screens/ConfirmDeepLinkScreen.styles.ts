import { StyleSheet } from "react-native";
import { spacing } from "../../../shared/design";

export const confirmDeepLinkStyles = StyleSheet.create({
  center: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: spacing[24],
  },
});
